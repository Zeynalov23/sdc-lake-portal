image:
  repository: <your-account-id>.dkr.ecr.eu-west-1.amazonaws.com/sdc-lake-dev/frontend
  tag: latest

dataServiceUrl: "http://data-service.app.svc.cluster.local"

entra:
  tenantId: ""
  clientId: ""
  # The client secret is NOT set here. It comes from External Secrets in
  # week 4; until then the deployment reads it from a Secret you create by
  # hand. A secret in values.yaml is a secret in git.
  secretName: sdc-lake-entra
  secretKey: client-secret
  redirectUri: ""
  postLogoutUri: ""
